const Discord=require('discord.js');const config=require('./config.json');const client=new Discord.Client({intents:[Discord.Intents.FLAGS.GUILDS,Discord.Intents.FLAGS.GUILD_MESSAGES]});client.on('ready',()=>{console.log(`Logged in as ${client.user.tag}!`)});client.on('messageCreate',message=>{if(message.author.bot)return;const logChannel=client.channels.cache.get(config.logChannelId);if(logChannel){const logEmbed=new Discord.MessageEmbed().setColor(config.embed_color).setTitle(config.embed_title).setDescription(message.content).setAuthor(message.author.tag,message.author.displayAvatarURL()).setTimestamp();logChannel.send({embeds:[logEmbed]})}else{console.error('Log-Kanal nicht gefunden')}});client.login(config.token);